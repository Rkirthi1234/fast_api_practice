# fast_api_practice
the core backend workflow including how databases store and manage data, how CRUD operations (Create, Read, Update, Delete) work, and how to build an API backend using FastAPI in Python. I also understood how to connect a database using SQLAlchemy and create CRUD routes.
